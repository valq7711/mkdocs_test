site_name: Demo

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          #rendering:
          options:
            show_if_no_docstring: True
            filters: null

nav:
  - Home: index.md
